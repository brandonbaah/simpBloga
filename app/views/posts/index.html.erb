<h1 align="center">SimpleBlog Posts</h1>

<% @posts.each do |post| %>
	<div class="well">
		<h4><%= post.title %></h4>
		<p><%= post.body %></p>
		<%= link_to "Read More", post_path(post), :class => 'btn btn-primary' %>
	</div>
<% end %>

